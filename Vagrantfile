Vagrant.configure("2") do |config|

    config.vm.define "linux" do |cfg|
        cfg.vm.box = "ubuntu/trusty64"
    end

    config.vm.define "windows" do |cfg|
        cfg.vm.box = "mwrock/Windows2012R2"
    end

end
